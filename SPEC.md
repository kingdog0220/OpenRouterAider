# openrouter_start.ps1 仕様書

## 1. 目的

Dockerコンテナ内でAiderを起動し、
ユーザーが指定したディレクトリのみを操作対象とすることで、
AIによる意図しないファイル操作を防止する。

## 2. 機能概要

本スクリプトは以下を行う。

1. 操作対象ディレクトリをユーザーに選択させる
2. OpenRouterで利用可能なモデルを選択させる
3. 選択されたディレクトリをDockerコンテナへマウントする
4. 選択されたモデルを指定してAiderを起動する

## 3. 画面仕様

### 入力項目

| 項目 | 内容 |
|--------|--------|
| 作業ディレクトリ | テキスト入力またはフォルダ選択ダイアログ |
| モデル | ドロップダウンリスト |
| OKボタン | Aider起動処理を実行 |
| キャンセルボタン | 処理を終了 |

### モデル一覧

ドロップダウンリストにはOpenRouter APIから取得したOpenRouterモデル一覧を表示する。

## 4. 処理仕様

### 起動時

- OpenRouterモデルを取得してドロップダウンリストに設定する

### OKボタン押下時

1. 入力内容を検証する
2. 起動中の対象Dockerコンテナを停止する
3. Dockerイメージをビルドする
4. 指定ディレクトリをマウントしてコンテナを起動する
5. 選択したモデルを指定してAiderを起動する

### キャンセルボタン押下時

処理を行わず終了する

## 5. 制約

- AIが編集できるのはマウントしたディレクトリ配下のみ
- コンテナ外のファイルは編集不可
- OpenRouter APIキーは環境変数から取得する

## 6. エラー処理

### ディレクトリ未指定

エラーメッセージを表示し処理を中断する

### Docker起動失敗

エラーメッセージを表示し処理を中断する

### Aider起動失敗

エラーメッセージを表示し処理を中断する
